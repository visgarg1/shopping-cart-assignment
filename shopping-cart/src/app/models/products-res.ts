export interface ProductsResponse {
        name: string;
        imagURL: string;
        descripton: string;
        price: number;
        stock: number;
        categry: string;
        sku: string;
        id: string;
}

