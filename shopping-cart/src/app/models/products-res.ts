export interface ProductsResponse {
        name: string;
        imagURL: string;
        descripton: string;
        price: number;
        stock: number;
        category: string;
        sku: string;
        id: string;
        number?: number;
}

