import { PasswordCustomValidatorDirective } from './password-custom-validator.directive';

describe('PasswordCustomValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordCustomValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
